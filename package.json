{
  "name": "bb-assistant-coach",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Tablet dashboard for coordinating team Blood Bowl events",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && tsc -p tsconfig.worker.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit && tsc -p tsconfig.worker.json",
    "deploy": "npm run build && wrangler deploy",
    "cf-login": "wrangler login",
    "db:migrate": "wrangler d1 migrations apply bb-coordinator --remote",
    "tourplay": "node scripts/fetch-tourplay.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "wrangler": "^4.131.1"
  }
}
